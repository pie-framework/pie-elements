{
  "description": "Model for the Choice Interaction",
  "type": "object",
  "properties": {
    "choices": {
      "description": "The choice options for the question",
      "type": "array",
      "items": {
        "$ref": "#/definitions/Choice"
      }
    },
    "prompt": {
      "description": "The question prompt or item stem",
      "type": "string"
    },
    "keyMode": {
      "$ref": "#/definitions/KeyMode",
      "description": "What key should be displayed before choices. If undefined no  key will be displayed."
    },
    "choiceMode": {
      "$ref": "#/definitions/ChoiceMode",
      "description": "Indicates the choices are single or multiple selection"
    },
    "id": {
      "description": "Identifier to identify the Pie Element in html markup, Must be unique within a pie item config.",
      "type": "string"
    },
    "element": {
      "description": "The html Element tag name",
      "type": "string"
    }
  },
  "required": [
    "choices",
    "element",
    "id"
  ],
  "definitions": {
    "Choice": {
      "type": "object",
      "properties": {
        "correct": {
          "description": "Indicates if the choice is correct",
          "type": "boolean"
        },
        "value": {
          "description": "the value that will be stored if this choice is selected",
          "type": "string"
        },
        "label": {
          "description": "the text label that will be presented to the user for this choice",
          "type": "string"
        },
        "feedback": {
          "$ref": "#/definitions/Feedback",
          "description": "student feedback for this choice, only shown in evaluate mode"
        }
      },
      "required": [
        "correct",
        "feedback",
        "label",
        "value"
      ]
    },
    "Feedback": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/definitions/FeedbackType",
          "default": "FeedbackType.default"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "value"
      ]
    },
    "FeedbackType": {
      "description": "The type of feedback to use:\n   `default` = a standard feedback message\n   `custom` = a customized feedback message",
      "enum": [
        "custom",
        "default"
      ],
      "type": "string"
    },
    "KeyMode": {
      "enum": [
        "letters",
        "numbers"
      ],
      "type": "string"
    },
    "ChoiceMode": {
      "enum": [
        0,
        1
      ],
      "type": "number"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}