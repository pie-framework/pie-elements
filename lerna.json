{
  "lerna": "3.11.0",
  "packages": ["packages/*", "packages/*/configure", "packages/*/controller"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["yarn.lock"],
      "message": "[skip travis] chore: publish"
    }
  }
}
